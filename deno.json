{
  "tasks": {
    "start": "cargo tauri dev",
    "build": "cargo tauri build",
    "start-ui": "cd src-ui && npm i && npm run dev",
    "build-ui": "cd src-ui && npm i && npm run build",
    "start-edge": "deno task build-ui && cd src-edge && deno task start"
  },
  "compilerOptions": {
  }
}
